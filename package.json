{
  "name": "zyyofuckyou",
  "version": "1.0.0",
  "author": "Zyyo",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0",
    "crypto-js": "^4.2.0",
    "express": "^5.1.0",
    "querystring": "^0.2.1",
    "ws": "^8.16.0"
  },
  "scripts": {
    "start": "node ./src/server.js",
    "build": "esbuild ./src/server.js --bundle --minify --outfile=./dist/dist.js --platform=node",
    "build:win": "npm run build && pkg . --targets node18-win-x64",
    "build:mac": "npm run build && pkg . --targets node18-macos-x64",
    "build:linux": "npm run build && pkg . --targets node18-linux-x64"
  },
  "bin": "./dist/dist.js",
  "pkg": {
    "assets": [
      "public/**/*"
    ]
  }
}